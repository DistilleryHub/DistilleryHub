{
  "name": "distilleryhub-rules-tests",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"mocha firestore.rules.test.js --timeout 20000\" --project demo-distilleryhub"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.7.3"
  }
}
